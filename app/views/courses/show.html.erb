<h1> Course Subject:  <%= @course.subject %> </h1>

<%= link_to 'Courses', courses_path %>
<%= link_to 'Edit Course', edit_course_path(@course) %>
<%= link_to("Delete Course", @course, :method => :delete, :class => 'action', data: {confirm: "Are you sure?"}) %>


<h2> Class Instructor: <%= @course.instructor %> </h2>

<%= @course.students %>
